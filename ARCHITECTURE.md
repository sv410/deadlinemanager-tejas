# 🏗️ DeadlineSync - System Architecture & Data Flow

## Overall Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         DEADLINE SYNC PLATFORM                          │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                           FRONTEND LAYER                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐  │
│  │   Landing    │  │  Dashboard   │  │  Analytics   │  │  Settings   │  │
│  │   Page       │  │  & Timeline  │  │  & Reports   │  │  & Profile  │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  └─────────────┘  │
│                                                                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐  │
│  │  Gamification│  │  Collaboration│ │ AI Insights  │  │   Timeline  │  │
│  │  Widget      │  │  Comments     │  │  Panel       │  │   View      │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  └─────────────┘  │
│                                                                           │
│  Technologies: Next.js 16, React 19, TypeScript, Tailwind CSS           │
│  UI Components: Radix UI, Shadcn UI, Recharts (for analytics)           │
└─────────────────────────────────────────────────────────────────────────┘
                                   │
                                   │ REST API Calls
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                            API LAYER                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ Authentication Routes                                              │ │
│  │ ├─ POST   /api/auth/google           → OAuth flow initiation      │ │
│  │ └─ GET    /api/auth/google/callback  → OAuth token exchange      │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ Deadline Management                                                │ │
│  │ ├─ GET    /api/deadlines              → Fetch user deadlines     │ │
│  │ ├─ POST   /api/deadlines              → Create deadline          │ │
│  │ ├─ PUT    /api/deadlines/:id          → Update deadline          │ │
│  │ └─ DELETE /api/deadlines/:id          → Delete deadline          │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ Gamification & Points (NEW)                                        │ │
│  │ ├─ GET    /api/gamification?userId    → Fetch user stats         │ │
│  │ ├─ POST   /api/gamification           → Award points            │ │
│  │ └─ PUT    /api/gamification           → Update streaks          │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ AI Insights & Timeline (NEW)                                       │ │
│  │ ├─ GET    /api/insights?userId        → Fetch AI insights       │ │
│  │ └─ POST   /api/insights               → Generate timeline view   │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ Analytics & Productivity (NEW)                                     │ │
│  │ ├─ GET    /api/analytics?userId&days  → Fetch analytics data    │ │
│  │ ├─ POST   /api/analytics              → Log daily metrics       │ │
│  │ └─ PUT    /api/analytics/:id          → Update analytics        │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ Deadline Sharing & Collaboration (NEW)                             │ │
│  │ ├─ GET    /api/deadline-sharing       → Fetch shared deadlines   │ │
│  │ ├─ POST   /api/deadline-sharing       → Share deadline           │ │
│  │ ├─ PUT    /api/deadline-sharing       → Update permissions      │ │
│  │ └─ DELETE /api/deadline-sharing       → Revoke access           │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ Comments & Communications (NEW)                                    │ │
│  │ ├─ GET    /api/deadline-comments      → Fetch comments          │ │
│  │ ├─ POST   /api/deadline-comments      → Add comment            │ │
│  │ ├─ PUT    /api/deadline-comments      → Update comment         │ │
│  │ └─ DELETE /api/deadline-comments      → Delete comment         │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ External Integrations                                              │ │
│  │ ├─ POST   /api/calendar/sync          → Sync to Google Calendar  │ │
│  │ ├─ POST   /api/calendar/meet          → Create Google Meet link  │ │
│  │ ├─ POST   /api/notifications/send     → Send email reminders     │ │
│  │ └─ GET    /api/contact                → Contact form submission  │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
│  Framework: Next.js 16 API Routes                                        │
│  Language: TypeScript                                                    │
│  Validation: Zod                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                                   │
                                   │ Supabase Client
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        DATABASE LAYER                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ Core Tables (Existing)                                            │  │
│  │ ├─ users              → User profiles & authentication            │  │
│  │ ├─ deadlines          → Deadline records                          │  │
│  │ ├─ google_tokens      → OAuth token storage                       │  │
│  │ ├─ google_calendar_events → Sync tracking                         │  │
│  │ └─ email_notifications    → Notification history                  │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ Gamification Tables (NEW - 4 tables)                              │  │
│  │ ├─ user_gamification        → Points, levels, streaks            │  │
│  │ ├─ user_achievements        → Unlocked achievements              │  │
│  │ ├─ user_activity_log        → Activity history                   │  │
│  │ └─ daily_challenges         → Pre-defined challenges             │  │
│  │     user_challenge_progress → User challenge progress            │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ Collaboration Tables (NEW - 6 tables)                             │  │
│  │ ├─ deadline_shares          → Share permissions                   │  │
│  │ ├─ deadline_comments        → Comments & discussions              │  │
│  │ ├─ team_members            → Team management                      │  │
│  │ ├─ productivity_analytics   → Daily analytics data               │  │
│  │ ├─ ai_insights             → AI-generated insights               │  │
│  │ └─ deadline_timeline_metadata → Timeline metadata               │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                           │
│  Database: Supabase (PostgreSQL)                                        │
│  Total Tables: 16 (5 existing + 11 new)                                 │
│  Indexes: 30+ performance optimizations                                 │
│  Backup: Automatic daily snapshots                                      │
└─────────────────────────────────────────────────────────────────────────┘
                                   │
                        External Services
                    ┌───────────────┼───────────────┐
                    ▼               ▼               ▼
        ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
        │ Google OAuth │  │ Google       │  │ Gmail SMTP   │
        │ & Calendar   │  │ Meet API     │  │ for Email    │
        └──────────────┘  └──────────────┘  └──────────────┘
```

---

## Data Flow Diagrams

### 1. User Registration & Login Flow
```
┌─────────────────────────────────────────────────────────────────┐
│ USER CLICKS "GET STARTED WITH GOOGLE"                           │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │ GET /api/auth/google         │
        │ Redirect to Google OAuth     │
        └──────────┬───────────────────┘
                   │
                   ▼
        ┌──────────────────────────────┐
        │ User logs in with Google     │
        │ Grants permissions           │
        └──────────┬───────────────────┘
                   │
                   ▼
        ┌──────────────────────────────┐
        │ GET /api/auth/google/callback│
        │ Exchange code for tokens     │
        └──────────┬───────────────────┘
                   │
                   ▼
        ┌──────────────────────────────┐
        │ Verify ID token              │
        │ Get user info from Google    │
        └──────────┬───────────────────┘
                   │
                   ├─ Check if user exists
                   │  └─ If NO: Create user ✨
                   │
                   ├─ Store Google tokens in DB
                   │  └─ google_tokens table
                   │
                   └─ Redirect to /dashboard
                      (NO email confirmation needed!)
```

### 2. Deadline Creation & Sync Flow
```
┌──────────────────────────────────────────────────────────────────┐
│ USER CREATES NEW DEADLINE                                        │
└──────────────────┬───────────────────────────────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────┐
        │ POST /api/deadlines          │
        │ - Title, description         │
        │ - Due date, priority         │
        └──────────┬───────────────────┘
                   │
        ┌──────────┴─────────────┬──────────────────┬──────────────┐
        ▼                        ▼                  ▼              ▼
    ┌────────┐          ┌──────────────┐  ┌──────────────┐  ┌─────────┐
    │ INSERT │          │ SYNC TO      │  │ SEND EMAIL   │  │ AWARD   │
    │ in DB  │          │ GOOGLE       │  │ NOTIFICATION │  │ 10 PTS  │
    └────────┘          │ CALENDAR ✨  │  │ (existing)   │  └─────────┘
                        └──────────────┘  └──────────────┘
        │                   │                  │              │
        └───────────────────┴──────────────────┴──────────────┘
                          │
                          ▼
        ┌──────────────────────────────────────┐
        │ UPDATE deadline_timeline_metadata    │
        │ - Calculate urgency score            │
        │ - Generate AI suggestion             │
        │ - Assess risk level                  │
        └──────────┬───────────────────────────┘
                   │
                   ├─ Update user_activity_log
                   │
                   ├─ Check for achievement unlock
                   │
                   └─ Return deadline with all data

RESULT: Deadline created, synced, notified, gamified! 🎉
```

### 3. Daily Analytics & Points Flow
```
┌──────────────────────────────────────────────────────────────────┐
│ DAILY CRON JOB (or manual trigger)                               │
│ Time: Usually run at night (11 PM)                               │
└──────────────────┬───────────────────────────────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────────┐
        │ POST /api/analytics                  │
        │ Log daily metrics:                   │
        │ - Deadlines created today            │
        │ - Deadlines completed today          │
        │ - Work minutes tracked               │
        │ - Focus score calculated             │
        └──────────┬───────────────────────────┘
                   │
        ┌──────────┴──────────────────┐
        │                              │
        ▼                              ▼
    ┌─────────────┐          ┌───────────────────┐
    │ STORE DATA  │          │ CALCULATE METRICS │
    │ in analytics│          │ - Completion rate │
    │ table       │          │ - Focus score     │
    └─────────────┘          │ - Best/worst day  │
                             └─────────────────┘
                                    │
        ┌───────────────────────────┴────────────────────┐
        │                                                 │
        ▼                                                 ▼
    ┌──────────────────┐                     ┌──────────────────┐
    │ HIGH FOCUS SCORE?│                     │ UPDATE STREAK    │
    │ (≥70)            │                     │ (if completing   │
    │ YES: Award       │                     │  daily deadline) │
    │ 10-100 bonus pts │                     └──────────────────┘
    └──────────────────┘                             │
        │                                           │
        ▼                                           ▼
    ┌──────────────────┐                     ┌──────────────────┐
    │ UPDATE           │                     │ 7-DAY STREAK?    │
    │ user_gamification│                     │ Award 500 bonus  │
    │ total_points     │                     │ points! 🎉       │
    └──────────────────┘                     └──────────────────┘

RESULT: Analytics logged, points awarded, streaks updated! 📊
```

### 4. AI Insight Generation Flow
```
┌──────────────────────────────────────────────────────────────────┐
│ USER VIEWS DASHBOARD / AI INSIGHT REQUESTED                      │
└──────────────────┬───────────────────────────────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────────┐
        │ GET /api/insights?userId=<id>        │
        └──────────┬───────────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────────┐
        │ ANALYZE USER PATTERNS:               │
        │                                      │
        │ 1. CHECK FOR OVERDUE DEADLINES       │
        │    └─ WARNING: "X overdue tasks"     │
        │                                      │
        │ 2. CHECK WORKLOAD (next 7 days)     │
        │    └─ WARNING: "Heavy week ahead"    │
        │                                      │
        │ 3. CHECK STREAK STATUS               │
        │    └─ CELEBRATION: "X-day streak!"   │
        │                                      │
        │ 4. DETECT PEAK HOURS                 │
        │    └─ SUGGESTION: "Work at X PM"     │
        │                                      │
        │ 5. CHECK COMPLETION RATE             │
        │    └─ RECOMMENDATION: "Break tasks"  │
        │                                      │
        │ 6. CHECK FIRST TIME USER             │
        │    └─ RECOMMENDATION: "Create first" │
        └──────────┬───────────────────────────┘
                   │
        ┌──────────┴──────────────────┐
        │                              │
        ▼                              ▼
    ┌─────────────────┐        ┌────────────────┐
    │ RETRIEVE        │        │ STORE IN DB    │
    │ EXISTING        │        │ ai_insights    │
    │ INSIGHTS        │        │ (7-day expiry) │
    │ (not expired)   │        └────────────────┘
    └─────────────────┘              │
        │                            │
        └────────────┬───────────────┘
                     │
                     ▼
        ┌──────────────────────────────────────┐
        │ RETURN INSIGHTS TO FRONTEND:         │
        │ - Active insights from DB            │
        │ - Newly generated insights           │
        │ - Grouped by priority (High→Low)     │
        └──────────────────────────────────────┘

RESULT: Personalized AI insights displayed to user! 🤖
```

### 5. Timeline View Generation Flow
```
┌──────────────────────────────────────────────────────────────────┐
│ USER VIEWS TIMELINE / URGENCY GROUPED VIEW                       │
└──────────────────┬───────────────────────────────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────────┐
        │ POST /api/insights                   │
        │ Request: Timeline view generation    │
        └──────────┬───────────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────────┐
        │ FETCH ALL USER DEADLINES             │
        │ Sorted by due date                   │
        └──────────┬───────────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────────┐
        │ CATEGORIZE BY URGENCY:               │
        │                                      │
        │ CRITICAL (RED)    ← Overdue or due  │
        │                     today             │
        │ URGENT (ORANGE)   ← Due in 1-3 days │
        │                                      │
        │ UPCOMING (YELLOW) ← Due in 4-7 days │
        │                                      │
        │ LATER (BLUE)      ← Due in 8+ days  │
        │                                      │
        │ COMPLETED (GREEN) ← Already done     │
        └──────────┬───────────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────────┐
        │ CALCULATE URGENCY SCORES:            │
        │ Formula: 100 - (days_until * 5)      │
        │ Range: 0-100 (higher = urgent)       │
        └──────────┬───────────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────────┐
        │ GENERATE AI SUGGESTIONS PER DEADLINE:│
        │                                      │
        │ Overdue  → "Complete immediately!"  │
        │ Due soon → "Start working on this"  │
        │ 1 week   → "Schedule time this week"│
        │ Later    → "Plan milestones"         │
        └──────────┬───────────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────────┐
        │ UPDATE deadline_timeline_metadata    │
        │ - urgency_score                      │
        │ - ai_risk_level                      │
        │ - ai_suggestion                      │
        └──────────┬───────────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────────┐
        │ RETURN TIMELINE VIEW:                │
        │ {                                    │
        │   timeline: {                        │
        │     critical: [...],                 │
        │     urgent: [...],                   │
        │     upcoming: [...],                 │
        │     later: [...],                    │
        │     completed: [...]                 │
        │   },                                 │
        │   summary: {                         │
        │     total: 15,                       │
        │     completed: 5,                    │
        │     pending: 10                      │
        │   }                                  │
        │ }                                    │
        └──────────────────────────────────────┘

RESULT: Deadlines grouped by urgency with AI insights! ⏱️
```

---

## Database Relationships

```
users
├── id (PK)
├── email
├── google_id
├── full_name
└── ... more fields

    ├─ 1:M ─ deadlines
    │        ├── id (PK)
    │        ├── user_id (FK → users)
    │        ├── title
    │        ├── due_date
    │        └── completed
    │           │
    │           ├─ 1:M ─ deadline_comments
    │           │        ├── id (PK)
    │           │        ├── deadline_id (FK)
    │           │        ├── user_id (FK)
    │           │        └── content
    │           │
    │           ├─ 1:M ─ deadline_shares
    │           │        ├── id (PK)
    │           │        ├── deadline_id (FK)
    │           │        ├── owner_id (FK)
    │           │        ├── shared_with_user_id (FK)
    │           │        └── share_type
    │           │
    │           ├─ 1:1 ─ google_calendar_events
    │           │        ├── id (PK)
    │           │        ├── deadline_id (FK)
    │           │        ├── calendar_event_id
    │           │        └── synced_at
    │           │
    │           └─ 1:1 ─ deadline_timeline_metadata
    │                    ├── id (PK)
    │                    ├── deadline_id (FK)
    │                    ├── urgency_score
    │                    └── ai_suggestion
    │
    ├─ 1:M ─ user_gamification
    │        ├── id (PK)
    │        ├── user_id (FK)
    │        ├── total_points
    │        ├── current_streak
    │        └── level
    │
    ├─ 1:M ─ user_achievements
    │        ├── id (PK)
    │        ├── user_id (FK)
    │        ├── achievement_id
    │        └── unlocked_at
    │
    ├─ 1:M ─ user_activity_log
    │        ├── id (PK)
    │        ├── user_id (FK)
    │        ├── activity_type
    │        └── points_earned
    │
    ├─ 1:M ─ productivity_analytics
    │        ├── id (PK)
    │        ├── user_id (FK)
    │        ├── date
    │        ├── completion_rate
    │        └── focus_score
    │
    ├─ 1:M ─ ai_insights
    │        ├── id (PK)
    │        ├── user_id (FK)
    │        ├── insight_type
    │        └── priority
    │
    ├─ 1:M ─ google_tokens
    │        ├── id (PK)
    │        ├── user_id (FK)
    │        ├── access_token
    │        └── refresh_token
    │
    └─ 1:M ─ team_members
             ├── id (PK)
             ├── team_id
             ├── user_id (FK)
             └── role

daily_challenges
├── id (PK)
├── challenge_name
└── ... more fields
    └─ 1:M ─ user_challenge_progress
             ├── id (PK)
             ├── challenge_id (FK)
             ├── user_id (FK)
             └── progress
```

---

## System Load & Performance

### Expected Traffic Patterns
```
Peak Hours:      9 AM, 2 PM, 6 PM
Average Users:   1,000-5,000
Peak Users:      10,000+
API Calls/Hour:  100,000+
Database Ops:    50,000+/hour
```

### Performance Targets
```
API Response Time:   < 200ms (95th percentile)
Timeline Generation: < 500ms
Analytics Query:     < 300ms
Database Query:      < 50ms
Page Load Time:      < 1.5s
```

### Scaling Strategy
```
1. Database: Supabase auto-scaling
2. API: Next.js serverless (Vercel)
3. CDN: Vercel edge caching
4. Images: Next.js image optimization
5. Analytics: Query caching (5-60 min TTL)
```

---

## Version History

**v1.0.0 (Current) - December 23, 2025**
- ✅ Core gamification system
- ✅ AI insights & timeline
- ✅ Analytics & productivity tracking
- ✅ Deadline sharing & collaboration
- ✅ Comments & communications
- ✅ Complete API implementation
- ✅ Production-ready database schema

**v0.9.0**
- Google OAuth integration
- Google Calendar sync
- Email notifications
- Core deadline management

**v0.8.0 & earlier**
- Landing page
- User authentication
- Basic deadline CRUD
- Contact forms

---

**Last Updated:** December 23, 2025  
**Status:** Production Ready ✅
